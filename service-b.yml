server:
  port: 8002
  
spring:
  cloud:
    consul:
      discovery:
        instanceId: serviceB
        health-check-interval: 5s
      config:
        management: 
          enabled: true
    stream:
      bindings:
        output:
          binder: kafka
          destination: cloudEX
management:
  security: 
    enabled: false
