server:
  port: 8811
  
spring:
  cloud:
    consul:
      discovery:
        instanceId: dashboard
        health-check-interval: 5s
      config:
        management: 
          enabled: true
    stream:
          bindings:
             sink-in-0:
                destination: cloudEX
management:
  security: 
    enabled: false
